[package]
name = "declmig-runner"
version = "0.1.0"
edition = "2021"
description = "Privileged, no-public-ingress execution plane for Declarative Migrations"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
